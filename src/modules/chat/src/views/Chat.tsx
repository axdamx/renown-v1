import React from 'react';
import {SafeAreaView, Text, View} from 'react-native';

const Chat = () => {
  return (
    <SafeAreaView>
      <Text>{'Chat'}</Text>
    </SafeAreaView>
  );
};

export default Chat;
